<%= form_with(model: reimbursement_form, local: true) do |form| %>
  <% if reimbursement_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reimbursement_form.errors.count, "error") %> prohibited this reimbursement_form from being saved:</h2>

      <ul>
      <% reimbursement_form.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :status_id %>
    <%= form.number_field :status_id, id: :reimbursement_form_status_id %>
  </div>

  <div class="field">
    <%= form.label :employee_id %>
    <%= form.number_field :employee_id, id: :reimbursement_form_employee_id %>
  </div>

  <div class="field">
    <%= form.label :trip_id %>
    <%= form.number_field :trip_id, id: :reimbursement_form_trip_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
