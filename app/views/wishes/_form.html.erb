<%= form_with(model: wish, local: true) do |form| %>
  <% if wish.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wish.errors.count, "error") %> prohibited this wish from being saved:</h2>

      <ul>
      <% wish.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :wish_type %>
    <%= form.text_field :wish_type, id: :wish_wish_type %>
  </div>

  <div class="field">
    <%= form.label :cost %>
    <%= form.text_field :cost, id: :wish_cost %>
  </div>

  <div class="field">
    <%= form.label :trip_id %>
    <%= form.number_field :trip_id, id: :wish_trip_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
